# Privacy

Memokaはローカルファーストで動作し、テレメトリー、広告、クラッシュレポートの自動送信を行いません。
ノート本文、タイトル、検索語、Clipboard、添付ファイル、WorkspaceのパスをMemokaのサーバーへ送信しません。

配布版は起動後に更新の有無を確認するため、GitHub ReleasesのHTTPS endpointへ接続します。この通信では
GitHubへIPアドレス等の通常のHTTP接続情報が伝わり得ます。更新のdownloadとinstallは利用者が`:update`で
明示的に確定した場合だけ実行します。

診断ログはOS標準のMemoka log directoryへローカル保存され、最大5 MiBのファイルを3世代まで保持します。
ログは自動送信されません。共有する場合は利用者自身が内容を確認してください。

Workspace内の`.memoka-backups/restic`には、ノートと添付ファイルの自動履歴を保存します。このローカル履歴には
パスワードを設定しません。Workspaceへアクセスできる人は過去世代も読めます。削除済みNote、過去の本文、
本文から参照されなくなった添付も保持対象なので、現在の表示から削除することと過去データの消去は同じではありません。

任意の追加保存先は利用者が選んだローカルディレクトリ、または明示設定したGoogle Driveです。暗号化用パスワードはOS資格情報ストアに保存し、
Restic子processの環境へ渡します。引数、通常ログ、設定ファイルやバックアップ内容には保存しません。
別環境での復旧に必要なパスワードは利用者自身でも安全に保管してください。追加先を解除しても既存repositoryは削除しません。

Google Driveは現在実験的で、実アカウントでの認証更新・別PC復旧は未検証です。公式AppImageとCLIにはMemoka用の
Desktop OAuth client設定を組み込みます。これはアプリの識別用設定であり、利用者のGoogle tokenやバックアップパスワードではありません。
client設定を同梱していても、利用者が接続を開始・認可するまでGoogleへ接続しません。
明示的な認可後だけ、同梱rcloneからGoogleへRestic暗号化済みのバックアップを送ります。
ローカル履歴と同じく、過去・Trash・本文から参照されなくなった添付も保存対象です。
scopeは`drive.file`で、アプリが扱えるファイルへの権限です。「特定の1フォルダーだけのOAuth権限」ではありません。
GoogleにはIP、API request、転送量、時刻、objectサイズ、専用フォルダー表示名等が伝わります。これらまで暗号化で隠すものではありません。

GoogleのtokenはWorkspace外の暗号化rclone configへ、復号鍵はOS資格情報ストアへ保存します。Resticパスワードとは別に管理し、
通常ログ、Workspace DB、バックアップ、frontend stateには含めません。Memoka独自のサーバーへ送る構成はありません。
資格情報ストアが利用不能なら処理を保留し、平文へ保存しません。同じOSユーザーが子process環境を閲覧できることまで防ぐものではありません。
「接続を解除」はこの端末の接続情報を削除/停止する操作です。Google側の認可取消は行わず、同じ接続の利用先があれば影響を確認します。
保存先解除はGoogleフォルダーを削除しません。保持整理で不要となったobjectはDriveのゴミ箱へ送り、Memokaがゴミ箱全体を空にすることはありません。
repository整理の完了とGoogle容量の回復は異なります。パスワードを失うと、Googleへ接続できてもバックアップ内容を復号できません。

転送の高速化のため、他ユーザーからのアクセスを制限した一時ディレクトリにResticの作業キャッシュを作ります。
転送ジョブ終了時に削除しますが、OSの異常終了や強制終了では残る場合があります。利用者の既存Resticキャッシュは使いません。
バックアップ画面の進捗情報は、工程・世代数・経過時間・通信量等の限定した情報をローカルで記録・表示します。
転送ツールの生ログ、ファイル名、tokenやパスワードを進捗情報として保存・表示しません。

CLIと履歴previewはNote内の外部URLを取得せず、添付も選んだWorkspace・世代の保存済みbytesだけを読みます。
同じWorkspaceを開いているprocessへのCLI接続は、同じOSユーザーだけが利用できるローカルIPCを使います。
