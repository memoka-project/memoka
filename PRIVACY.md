# Privacy

Memokaはローカルファーストで動作し、テレメトリー、広告、クラッシュレポートの自動送信を行いません。
ノート本文、タイトル、検索語、Clipboard、添付ファイル、WorkspaceのパスをMemokaのサーバーへ送信しません。

配布版は起動後に更新の有無を確認するため、GitHub ReleasesのHTTPS endpointへ接続します。この通信では
GitHubへIPアドレス等の通常のHTTP接続情報が伝わり得ます。更新のdownloadとinstallは利用者が`:update`で
明示的に確定した場合だけ実行します。

診断ログはOS標準のMemoka log directoryへローカル保存され、最大5 MiBのファイルを3世代まで保持します。
ログは自動送信されません。共有する場合は利用者自身が内容を確認してください。

Workspace内の`.memoka-backups/restic`には、ノートと添付ファイルの自動履歴を保存します。このローカル履歴には
パスワードを設定しません。Workspaceへアクセスできる人は過去世代も読めます。削除済みNote、過去の本文、
本文から参照されなくなった添付も保持対象なので、現在の表示から削除することと過去データの消去は同じではありません。

任意の追加保存先は利用者が選んだローカルディレクトリです。暗号化用パスワードはOS資格情報ストアに保存し、
Restic子processの環境へ渡します。引数、通常ログ、設定ファイルやバックアップ内容には保存しません。
別環境での復旧に必要なパスワードは利用者自身でも安全に保管してください。追加先を解除しても既存repositoryは削除しません。

CLIと履歴previewはNote内の外部URLを取得せず、添付も選んだWorkspace・世代の保存済みbytesだけを読みます。
同じWorkspaceを開いているprocessへのCLI接続は、同じOSユーザーだけが利用できるローカルIPCを使います。
