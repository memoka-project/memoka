export function BackupNotices({
  google,
  messages,
}: {
  google: boolean;
  messages: Readonly<Record<string, string>>;
}) {
  return (
    <aside className="backup-notices" aria-label="注意事項" data-modal-scroll>
      <h3>
        <span aria-hidden="true">⚠ </span>注意事項
      </h3>
      {Object.entries(messages).map(([id, message]) => (
        <p className="backup-notice-important" key={id}>
          {message}
        </p>
      ))}
      <details>
        <summary>世代数・パスワード・保存先について</summary>
        <ul>
          <li>
            世代数はノート数ではなく履歴の世代数です。「検証済み／転送済み／保存対象」の順で、転送済みには検証待ちも含みます。ローカル履歴では保存・検証が完了した世代数を共通で表示します。件数は最後に確認できた状態で、不明な場合は「—」です。
          </li>
          <li>
            最終保存は検証済み世代の元の保存日時です。転送時刻とは異なります。登録・予約・転送完了だけでは検証済みになりません。
          </li>
          <li>
            ローカル履歴は同じディスクの故障には備えられません。追加先のパスワードはOS資格情報ストアへ保存しますが、復旧に備えて別途保管してください。既存パスワードの再登録は、暗号化パスワードの変更ではありません。
          </li>
          <li>
            保持条件はいずれかに該当する世代を残します。日次・月次の0はその条件を無効にします。保存先の登録解除は、保存済みバックアップを削除しません。
          </li>
          <li>
            通信量は読み書き合計で、進捗率や残り時間ではありません。終了時には未完了の処理を中断し、次回起動後に再開します。
          </li>
          <li>
            バックアップ処理でロック競合を検出した場合は、失効ロックを自動解除して一度だけ再試行します。通常の画面表示・状態更新ではロックを調べません。失効の判定はResticに従い、同じホスト名の終了済みPID、または30分を超えて更新されていないロックが対象です。正常なプロセスはロックを定期更新します。使用中のロックは強制解除しません。Workspace
            IDは復旧後も同じですが、追加保存先の登録は復旧では引き継ぎません。今後の端末同期でも同期しない方針です。
          </li>
          {google && (
            <>
              <li>
                Google
                Driveは実験的です。実Googleアカウントでの長期認証・別PC復旧は未検証のため、重要なデータは別の保存先にも残してください。
              </li>
              <li>
                Googleへの接続には明示的な認可が必要です。権限は{" "}
                <code>drive.file</code>{" "}
                のみですが、専用フォルダーだけに限定されたOAuth権限ではありません。
              </li>
              <li>
                Google接続とバックアップ用パスワードは別です。復旧用情報とパスワードを別途保管してください。接続解除はGoogleの認可取消やフォルダー削除を行いません。
              </li>
              <li>
                保持整理で不要になったデータはDriveのゴミ箱へ移ります。使用容量がすぐ減るとは限らず、Memokaはゴミ箱を空にしません。登録を取り消しても作成済みフォルダーは残ります。
              </li>
            </>
          )}
        </ul>
      </details>
    </aside>
  );
}
